Place the two image files you attached here with the exact filenames below so the landing page can display them:

- attachment-phone-1.png  -> used at /assets/attachment-phone-1.png
- attachment-phone-2.png  -> used at /assets/attachment-phone-2.png

Steps:
1. Copy the provided phone mockup images into this folder.
2. Restart the dev server if it's running (`npm run dev`).

Notes:
- Vite serves files from the `public` directory at the project root. Use the paths shown above when referencing from React components.
- If you want different filenames, update the `src/components/LandingPage.jsx` image `src` attributes accordingly.
